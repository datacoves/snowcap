name: account-parameters-snowcap-example
# Note: run_mode and allowlist are not currently supported in config

account_parameters:
  - name: ALLOW_CLIENT_MFA_CACHING
    value: true
  - name: ALLOW_ID_TOKEN
    value: true
  - name: TIMEZONE
    value: "America/New_York"